[Unit]
Description=Activate saved wired network profiles or request DHCP
After=NetworkManager.service
Requires=NetworkManager.service

[Service]
Type=oneshot
ExecStart=/usr/libexec/xur-network
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
