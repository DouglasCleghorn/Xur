#!/usr/bin/bash
set -euo pipefail
case "${1:-}" in control|agent|gateway) name=$1;; *) exit 2;; esac
bundle=/run/xur/app/current
test -f "$bundle/bundle.json" || bundle=/usr/share/xur/app-bundle
export XUR_CATALOG="$bundle/catalog"
cd "$bundle/$name"
exec "./Xur.${name^}"
